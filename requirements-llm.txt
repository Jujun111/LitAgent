# Optional experimental vLLM fallback dependencies.
# llama.cpp is the primary GGUF inference server and is installed as a standalone binary.
# Install this only if you want to reproduce the vLLM fallback experiment.

-r requirements.txt

vllm>=0.12.0
